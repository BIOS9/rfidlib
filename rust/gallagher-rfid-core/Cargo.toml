[package]
name = "gallagher-rfid-core"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[features]
default = ["std"]
std = []

[dependencies]
aes = { workspace = true }
heapless = { workspace = true }
