[package]
name = "tapsmith-core"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[features]
default = ["std"]
std = []

[dependencies]
aes = { workspace = true }
des = { workspace = true }
heapless = { workspace = true }
